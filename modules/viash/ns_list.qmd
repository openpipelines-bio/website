---
title: "Ns list"
namespace: "Viash"
description: "Generates .qmd files for documentating viash components and workflows"
type: "module"
toc: false
---

::: {.column-margin}

### Info

ID: `ns_list`  
Namespace: `viash`

### Links
[Source](https://github.com/openpipelines-bio/openpipeline/blob/experiment_website_push/viash_tools/src/viash/ns_list){.btn-action .btn-sm .btn-info .btn role="button"}

:::



## Example commands

You can run the pipeline using `nextflow run`.

### View help

You can use `--help` as a parameter to get an overview of the possible parameters.

```bash
nextflow run openpipelines-bio/openpipeline \
  -r experiment_website_push -latest \
  -main-script target/nextflow/viash/ns_list/main.nf \
  --help
```

### Run command

<details>
<summary>Example of `params.yaml`</summary>
```yaml

```
</details>

```bash
nextflow run openpipelines-bio/openpipeline \
  -r experiment_website_push -latest \
  -profile docker \
  -main-script target/nextflow/viash/ns_list/main.nf \
  -params-file params.yaml
```

:::{.callout-note}
Replace `-profile docker` with `-profile podman` or `-profile singularity` depending on the desired backend.
:::

## Argument group

### Arguments

|Name       |Description                                                       |Attributes                                  |
|:----------|:-----------------------------------------------------------------|:-------------------------------------------|
|`--input`  |Repository url (currently only public repositories are supported) |`string`, required, example: `"input.yaml"` |
|`--output` |yaml file from `viash ns list`                                    |`file`, required, example: `"output.yaml"`  |
|`--branch` |Git branch to parse                                               |`string`, default: `"main"`                 |

## Authors

  * Dries Schaumont [{{< fa brands github >}}](https://github.com/DriesSchaumont) (maintainer)

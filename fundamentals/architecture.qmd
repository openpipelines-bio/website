---
title: Architecture
description: How OpenPipelines fits into a single-cell project
order: 30
---

OpenPipelines provides modular, best-practice Nextflow workflows for single-cell data processing — from raw sequencing output to a batch-corrected, clustered dataset. The diagram below shows how OpenPipelines fits into a typical project, what is covered by the core workflows, and what sits outside.

```{mermaid}
%%| label: fig-architecture
%%| fig-cap: "Overview of a single-cell project. Demultiplexing and downstream analyses sit outside the OpenPipelines core; ingestion, sample processing, and integration are handled by OpenPipelines core workflows."
flowchart TD
  classDef external fill:#f5f5f5,stroke:#aaaaaa,color:#444
  classDef prewf   fill:#fff3cd,stroke:#c9a227,color:#333
  classDef ingest  fill:#d5e8d4,stroke:#82b366,color:#000
  classDef process fill:#dae8fc,stroke:#6c8ebf,color:#000
  classDef integ   fill:#ffe6cc,stroke:#d6b656,color:#000
  classDef down    fill:#f8cecc,stroke:#b85450,color:#000

  wetlab["Experiment & sequencing\n(10x Chromium · BD Rhapsody · ...)"]:::external
  bcl["BCL files"]:::external
  demux["ingestion/demux\nBCL → FASTQ"]:::prewf
  fastq["FASTQ files\n(or pre-built count tables)"]:::external

  subgraph OP["  OpenPipelines — Core Workflows  "]
    direction TB
    ingest["Ingestion\ncellranger_mapping · cellranger_multi\nbd_rhapsody · ingestion/conversion\n→ H5MU per sample"]:::ingest
    process["Process samples\nrna_singlesample · prot_singlesample · concat\nrna_multisample · prot_multisample\n→ QC · filter · norm · HVF · PCA"]:::process
    integ["Integration\nharmony_leiden · scvi_leiden · scanorama_leiden\nbbknn_leiden · totalvi_leiden\n→ batch correction · Leiden clustering · UMAP"]:::integ
  end

  downstream["Downstream & annotation\ncelltypist · singler · popv · harmony_knn\nDE · RNA velocity · LIANA · BEYOND\n(optional · separate · mostly outside OP)"]:::down

  wetlab --> bcl
  bcl --> demux --> fastq
  wetlab -.->|"FASTQ or\ncount tables"| fastq
  fastq --> ingest
  ingest --> process
  process --> integ
  integ --> downstream
```

## Demultiplexing

If your sequencing provider delivers raw Illumina BCL files, convert them to per-sample FASTQ files before running OpenPipelines. This step sits **outside the core workflow** and is run independently.

The `ingestion/demux` workflow supports bcl2fastq, BCL Convert, and Cell Ranger mkfastq. If you already have FASTQ files or a pre-built count matrix, skip directly to [Ingestion](#ingestion).

## Ingestion

Every cell in a single-cell experiment is tagged with a unique barcode before sequencing. Ingestion aligns the resulting short reads back to a reference genome, uses the barcode to assign each read to a cell, and uses the UMI (Unique Molecular Identifier) to count each RNA molecule exactly once — removing the amplification bias from PCR. The result is a **count matrix**: cells × genes, with each entry being the number of RNA molecules detected.

OpenPipelines stores this count matrix — along with cell and gene metadata and any additional modalities (ADT, VDJ, ATAC) — in a single **H5MU file per sample**. This per-sample H5MU is the unit that flows through all subsequent steps. The right ingestion workflow depends on your sequencing platform and library type. See [Ingestion](../user_guide/ingestion.qmd) for workflow selection, reference genome setup, and examples.

## Process samples

Raw count matrices contain not only real cells but also empty droplets (barcodes that captured ambient RNA), damaged cells (high mitochondrial fraction), and doublets (two cells sequenced as one). **Per-sample QC** filters these out before samples are combined.

Samples are then **concatenated and normalized together** — not per sample — because normalization methods like HVF selection and PCA need to see variation across the full cohort to identify biologically meaningful genes. The output of this stage is a single multi-sample H5MU with:

- filtered, normalized count matrices per modality
- a set of **highly variable features** (HVF) selected across the cohort
- a **PCA embedding** — the compressed representation that all integration methods operate on

`multiomics/process_samples` wraps all per-sample and multi-sample steps into a single command. For large cohorts, `multiomics/process_batches` adds a batching layer that processes groups of samples in parallel before merging, reducing peak memory. See [Processing](../user_guide/processing.qmd) for full details and examples.

:::{.callout-note}
ATAC data is carried through `process_samples` without dedicated QC or normalization — ATAC-specific processing workflows are not yet implemented. ATAC ingestion (`ingestion/cellranger_atac_count`) and a standalone QC metrics module (`calculate_atac_qc_metrics`) are available.
:::

## Integration

When samples come from different donors, sequencing runs, or library preparations, cells of the same biological type will cluster by their technical origin rather than their biology. **Batch correction** realigns these cells in the PCA embedding so that biological similarity — not technical source — drives the cell groupings.

From the corrected embedding, a **k-nearest-neighbor graph** is built: each cell is connected to its most similar neighbours. Leiden community detection runs on this graph to assign **cluster labels**, and UMAP projects the graph into 2D for visualization. These three outputs — corrected embedding, cluster labels, UMAP — are what annotation and downstream tools consume.

OpenPipelines provides one workflow per integration method. There is no single default — the right choice depends on dataset size and batch structure. See [Processing — Integration](../user_guide/processing.qmd#integration-and-batch-correction) for guidance on choosing a method.

## Downstream and annotation

After integration, the numbered Leiden clusters have no biological labels yet. **Annotation** assigns cell type identities — either by comparing marker gene expression to known references, or by transferring labels from an annotated atlas. This step is always run separately from the core pipeline and depends on the tissue and biological question.

Beyond annotation, the integrated H5MU is the starting point for differential expression, RNA velocity, cell-cell communication, and population-level analyses. Some of these are available as OpenPipelines workflows; others are external tools that consume the H5MU directly. See [Downstream](../user_guide/downstream.qmd) for details and examples.

See the [Components reference](../components/) for the full argument list of each workflow and component.

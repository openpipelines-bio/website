```{=html}
<ul>
  <% for (const item of items) { %>
  <li>
    <a href="<%- item.path %>">
    <%= item.title %>
    <% if (item.subtitle) { %>
    : <%= item.subtitle %>
    <% } %>
    </a>
  </li>
  <% } %>
</ul>
```